# Week 5: Information Flow in GT and DB

Files:

- `lecture9.tex` (Beamer source)
- `lecture9_val_loss.png` (comparison figure)
- `lecture9_val_ppl.png` (comparison figure)

Compile (from this folder):

```bash
pdflatex lecture9.tex
```
